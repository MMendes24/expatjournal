const initialState = {
    isLoading: false,
    stories: [],
    error: '',
}


const storyReducer = ( state = initialState, action ) => {
    switch(action.type){
        default: return state
    }
}
export default storyReducer
